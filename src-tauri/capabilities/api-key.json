{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "api-key-capability",
  "description": "Capability for API key storage and retrieval",
  "windows": ["main"],
  "permissions": [
    "api-key-permissions"
  ]
}